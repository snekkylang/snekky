Thread.run(func() {
    mut fib = null;
    fib = func(n) {
        return if n < 2 {
            n
        } else {
            fib(n - 1) + fib(n - 2)   
        };
    };


    Sys.println(fib(20));
});

Sys.println("before");